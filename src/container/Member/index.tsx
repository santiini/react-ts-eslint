import React, {FC} from 'react';
import {NamedRouteComponentProps} from 'RouteComponent';

const Member: FC<NamedRouteComponentProps> = (props) => {
  return (
    <div>
      <h4>Member</h4>
    </div>
  );
};

export default Member;
